import { Link } from 'react-router';
import Component11CourseCatalog from '../imports/11CourseCatalog';

// We'll use the imported component structure but need to hide the debug screen ID
// and ensure all navigation uses React Router

export default function CourseCatalog() {
  return <Component11CourseCatalog />;
}
